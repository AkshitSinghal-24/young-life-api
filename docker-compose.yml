version: '3.8'

services:
  crowd-counter:
    build: .
    ports:
      - "80:80"
    volumes:
      - ./temp_uploads:/app/temp_uploads
      - ./results:/app/results
      - ./weights:/app/weights
    restart: unless-stopped
    environment:
      - WORKERS=1
